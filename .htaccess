<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Jangan arahkan ulang jika request sudah mengarah ke folder public/
    RewriteCond %{REQUEST_URI} !^/public/
    
    # Arahkan ulang semua request ke folder public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
